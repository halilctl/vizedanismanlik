{
  "name": "visa-consulting-website",
  "version": "1.0.0",
  "description": "Modern visa consulting company website",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:custom": "cross-env VITE_USE_CUSTOM_DOMAIN=true vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "deploy:custom": "npm run build:custom && gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "framer-motion": "^10.16.4",
    "react-intersection-observer": "^9.5.2",
    "@react-three/fiber": "^8.15.11",
    "@react-three/drei": "^9.88.13",
    "three": "^0.158.0",
    "@emailjs/browser": "^4.4.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0",
    "gh-pages": "^6.1.0",
    "cross-env": "^7.0.3"
  }
}

